 {/* <FlatList
          data={CATEGORIES}
          ListHeaderComponent={() =>
            headingTitle({
              title: "Categories",
              onPress: (item) =>
                navigate("Categories", { selectedCategory: item }),
            })
          }
          renderItem={({ item }) => (
            <Categories
              item={item}
              onPress={() => navigate("Categories", { selectedCategory: item })}
            />
          )}
          keyExtractor={(item) => item.id}
          horizontal
          showsHorizontalScrollIndicator={false}
        />  */}

        {/* <FlatList
          data={CATEGORIES}
          ListHeaderComponent={() =>
            headingTitle({
              title: "Categories",
              onPress: () => navigate("Categories",{ selectedCategory: 'sadas' }),
            })
          }
          renderItem={({ item }) => (
            <Categories
              item={item}
              onPress={() => navigate("Categories", { selectedCategory: item })}
            />
          )}
          keyExtractor={(item) => item.id.toString()} // Ensure key is converted to string
          horizontal
          showsHorizontalScrollIndicator={false}
        /> */}
